meta {
  name: Update-Contact
  type: http
  seq: 3
}

put {
  url: {{URL}}/api/admin/contacts/your_contact_id_here
  body: json
  auth: bearer
}

auth:bearer {
  token: {{accessToken}}
}

body:json {
  { "label": "LinkedIn",
   "value": "linkedin.com/in/wtfoong",
   "sort_order": 5 }
}

settings {
  encodeUrl: true
  timeout: 0
}
