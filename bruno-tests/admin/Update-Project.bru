meta {
  name: Update-Project
  type: http
  seq: 21
}

put {
  url: {{URL}}/api/admin/projects/your_project_id_here
  body: json
  auth: bearer
}

auth:bearer {
  token: {{accessToken}}
}

body:json {
  { "is_published": false }
}

settings {
  encodeUrl: true
  timeout: 0
}
