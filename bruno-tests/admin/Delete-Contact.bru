meta {
  name: Delete-Contact
  type: http
  seq: 4
}

delete {
  url: {{URL}}/api/admin/contacts/your_contact_id_here
  body: none
  auth: bearer
}

auth:bearer {
  token: {{accessToken}}
}

settings {
  encodeUrl: true
  timeout: 0
}
