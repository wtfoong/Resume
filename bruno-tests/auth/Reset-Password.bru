meta {
  name: Reset-Password
  type: http
  seq: 5
}

post {
  url: {{URL}}/api/auth/reset-password
  body: json
  auth: none
}

body:json {
  { "token": "your_reset_token_here",
   "newPassword": "your_new_password_here" }
}

settings {
  encodeUrl: true
  timeout: 0
}
