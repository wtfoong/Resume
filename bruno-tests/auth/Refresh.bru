meta {
  name: Refresh
  type: http
  seq: 2
}

post {
  url: {{URL}}/api/auth/refresh
  body: json
  auth: none
}

body:json {
  { "refreshToken": "{{refreshToken}}" }
}

settings {
  encodeUrl: true
  timeout: 0
}
