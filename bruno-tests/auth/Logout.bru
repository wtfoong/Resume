meta {
  name: Logout
  type: http
  seq: 3
}

post {
  url: {{URL}}/api/auth/logout
  body: json
  auth: none
}

body:json {
  { "refreshToken": "{{refreshToken}}" }
}

settings {
  encodeUrl: true
  timeout: 0
}
